const usersLoginValidator = require('./users/login.validator')
const usersRegisterValidator = require('./users/register.validator')

module.exports = {
    usersLoginValidator,
    usersRegisterValidator
}